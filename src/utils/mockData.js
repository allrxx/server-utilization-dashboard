export const mockData1 = {
    cpuUtilization: Array.from({ length: 12 }, () => Math.random()),
  };
  
  export const mockData2 = {
     // Modify the categories array to contain the names of the 7 days of the week
    cpuUtilization: Array.from({ length: 7 }, () => Math.random() * 100), // Modify the cpuUtilization array to contain 7 random numbers between 0 and 100
  };
  
  export const mockData3 = {
    cpuUtilization: Array.from({ length: 24 }, () => Math.random() * 100),
  };
  
  export const mockData4 = {
    cpuUtilization: Array.from({ length: 12 }, () => Math.random()),
  };
  